import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hinario.tabernaculoavozdocristao',
  appName: 'Hinário Tabernáculo A Voz do Cristão',
  webDir: 'dist'
};

export default config;
